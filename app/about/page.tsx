import { Page } from '@/components/layout/Page';

export default function AboutPage() {
  return (
    <Page heading="About">
      This is a simple test project to demonstrate how to use Next.js with NextUI, Prisma ORM and VercelPostgres DB. It
      includes a simple project structure with separated features (not FSD). The project also includes a simple form to
      add new items to the list.
    </Page>
  );
}
